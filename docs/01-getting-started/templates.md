# راهنمای تمپلت‌ها

WeWork Framework شامل 5 تمپلت آماده است که می‌توانید برای شروع سریع پروژه‌های مختلف از آن‌ها استفاده کنید.

## 📋 تمپلت‌های موجود

### 1. Basic Template (پیش‌فرض)

تمپلت پایه و ساده برای شروع از صفر:

```bash
wework create my-project --template basic
# یا
wework create my-project
```

**ویژگی‌ها:**
- ✅ ساختار کامل Backend (FastAPI)
- ✅ ساختار کامل Frontend (React + Vite)
- ✅ Docker Compose
- ✅ فایل‌های تنظیمات
- ✅ مناسب برای پروژه‌های سفارشی

### 2. Corporate Template

وب‌سایت شرکتی مدرن با صفحات کامل:

```bash
wework create my-corporate-site --template corporate
```

**ویژگی‌ها:**
- ✅ لندینگ مدرن با Hero Section
- ✅ صفحه خدمات (Services)
- ✅ صفحه درباره ما (About)
- ✅ صفحه تماس با ما (Contact)
- ✅ Navigation Bar حرفه‌ای با منوی موبایل
- ✅ طراحی Mobile-First
- ✅ Backend API برای فرم تماس

**صفحات:**
- `/` - صفحه اصلی
- `/services` - خدمات
- `/about` - درباره ما
- `/contact` - تماس با ما

### 3. E-commerce Template

فروشگاه آنلاین کامل با تمام قابلیت‌ها:

```bash
wework create my-online-store --template ecommerce
```

**ویژگی‌ها:**
- ✅ صفحه محصولات با جستجو
- ✅ صفحه جزئیات محصول
- ✅ سبد خرید (Shopping Cart)
- ✅ صفحه پرداخت (Checkout)
- ✅ Navigation با آیکون سبد خرید
- ✅ Context API برای مدیریت سبد خرید
- ✅ طراحی Mobile-First

**صفحات:**
- `/` - صفحه اصلی
- `/products` - لیست محصولات
- `/products/:id` - جزئیات محصول
- `/cart` - سبد خرید
- `/checkout` - پرداخت

**مدل‌های دیتابیس:**
- Product
- Order
- User

### 4. Admin Panel Template

پنل مدیریتی حرفه‌ای با داشبورد و جداول:

```bash
wework create my-admin-panel --template admin
```

**ویژگی‌ها:**
- ✅ Dashboard با آمار و نمودارها
- ✅ جداول داده با جستجو
- ✅ Sidebar Navigation
- ✅ طراحی Responsive
- ✅ Backend API برای آمار و مدیریت

**صفحات:**
- `/` - داشبورد
- `/users` - مدیریت کاربران

**ویژگی‌های طراحی:**
- Sidebar ثابت
- کارت‌های آمار
- جداول با قابلیت جستجو
- طراحی Dark Mode آماده

### 5. Messaging Template

سامانه ارسال پیام برای پیام‌رسان‌های ایرانی:

```bash
wework create my-messaging-app --template messaging
```

**ویژگی‌ها:**
- ✅ لندینگ جذاب با پلن‌های اشتراک
- ✅ امکان خرید اشتراک
- ✅ مدیریت اکانت‌های پیام‌رسان (بله، ایتا، روبیکا، سروش، گپ)
- ✅ ارسال پیام به صورت توزیع شده بین چندین اکانت
- ✅ مدیریت کمپین‌های ارسال
- ✅ پنل مدیریت کامل
- ✅ داشبورد کاربری با آمار

**صفحات کاربری:**
- `/` - لندینگ با پلن‌ها
- `/dashboard` - داشبورد کاربری
- `/dashboard/accounts` - مدیریت اکانت‌ها
- `/dashboard/campaigns` - کمپین‌های ارسال
- `/dashboard/campaigns/new` - ایجاد کمپین جدید

**صفحات مدیریتی:**
- `/admin` - داشبورد مدیریت
- `/admin/users` - مدیریت کاربران

**مدل‌های دیتابیس:**
- User
- SubscriptionPlan
- Subscription
- MessengerAccount
- MessageCampaign
- MessageLog

**قابلیت‌های ویژه:**
- اتصال چندین اکانت پیام‌رسان به یک کاربر
- توزیع خودکار پیام‌ها بین اکانت‌ها
- مدیریت اشتراک‌ها با پلن‌های مختلف
- تاریخچه کامل ارسال پیام

## 🚀 استفاده از تمپلت‌ها

### روش 1: انتخاب از منوی تعاملی

```bash
wework create my-project
```

سپس از منوی نمایش داده شده تمپلت مورد نظر را انتخاب کنید:

```
📋 Available templates:

  basic        - Basic Starter        - Basic starter template with minimal setup
  corporate    - Corporate/Company    - Modern corporate website template with services, about, contact pages
  ecommerce    - E-commerce/Store      - Complete e-commerce template with products, cart, checkout
  admin        - Admin Panel          - Professional admin dashboard with tables, charts, and analytics
  messaging    - Messaging System     - Complete messaging system for Iranian messengers with subscription, account management, and distributed messaging

Select template [basic]:
```

### روش 2: مشخص کردن مستقیم تمپلت

```bash
wework create my-project --template corporate
wework create my-project --template ecommerce
wework create my-project --template admin
wework create my-project --template messaging
wework create my-project --template basic
```

## 📝 مراحل بعد از ایجاد پروژه

بعد از ایجاد پروژه از تمپلت:

```bash
# 1. رفتن به پوشه پروژه
cd my-project

# 2. تنظیم متغیرهای محیطی
cp .env.example .env
# فایل .env را ویرایش کنید

# 3. نصب وابستگی‌های بک‌اند
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt

# 4. راه‌اندازی دیتابیس و Redis
docker-compose up -d postgres redis

# 5. ایجاد جداول دیتابیس
python -m src.db.create_tables

# 6. راه‌اندازی بک‌اند
uvicorn src.api.app:app --reload --port 8000

# 7. در ترمینال جدید - راه‌اندازی فرانت‌اند
cd frontend
npm install
npm run dev
```

## 🎨 سفارشی‌سازی تمپلت‌ها

تمپلت‌ها به عنوان نقطه شروع هستند و می‌توانید آن‌ها را کاملاً سفارشی کنید:

### اضافه کردن API جدید

```bash
wework make:api new-feature
```

### اضافه کردن Model جدید

```bash
wework make:model NewModel
```

### اضافه کردن Component جدید

```bash
wework make:component NewComponent
```

### اضافه کردن Hook جدید

```bash
wework make:hook useNewFeature
```

## 💡 نکات مهم

1. **تمپلت‌ها نقطه شروع هستند**: همه تمپلت‌ها قابل ویرایش و سفارشی‌سازی هستند.

2. **استفاده از CLI**: بعد از ایجاد پروژه، می‌توانید از CLI برای اضافه کردن المان‌های جدید استفاده کنید.

3. **مستندات تمپلت**: هر تمپلت دارای README.md مخصوص خود است که در پوشه پروژه قرار دارد.

4. **Mobile-First**: همه تمپلت‌ها با رویکرد Mobile-First طراحی شده‌اند.

5. **Performance**: تمام تمپلت‌ها بهینه‌سازی شده و Performance بالایی دارند.

## 🔄 تغییر تمپلت

اگر می‌خواهید از یک تمپلت به تمپلت دیگر تغییر دهید، می‌توانید:

1. پروژه جدید با تمپلت مورد نظر ایجاد کنید
2. فایل‌های مورد نیاز را کپی کنید
3. یا مستقیماً فایل‌های تمپلت را از `src/cli/templates/` بررسی کنید

## 📚 مثال‌های استفاده

### مثال 1: ایجاد وب‌سایت شرکتی

```bash
wework create my-company --template corporate
cd my-company
# سپس مراحل راه‌اندازی را انجام دهید
```

### مثال 2: ایجاد فروشگاه آنلاین

```bash
wework create my-shop --template ecommerce
cd my-shop
# سپس مراحل راه‌اندازی را انجام دهید
```

### مثال 3: ایجاد سامانه پیام

```bash
wework create my-messaging --template messaging
cd my-messaging
# سپس مراحل راه‌اندازی را انجام دهید
```

---

برای اطلاعات بیشتر، به [راهنمای CLI](../CLI.md) مراجعه کنید.

